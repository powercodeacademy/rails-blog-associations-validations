<p id="notice"><%= notice %></p>
<p>
  <h3>Name: <%= @post.name %></h3>
  <small>Tags: <%= @post.tags.map(&:name).join(', ') %></small>
  <p><%= @post.content %></p>
</p>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
